# Replace with your actual domain (e.g., api.yourname.com)
https://emotion-speech-recognition.vercel.app {
    # Proxy requests to the 'api' service defined in docker-compose
    reverse_proxy api:8000

    # Optional: Log requests
    log {
        output file /data/access.log
    }
}
